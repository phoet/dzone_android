<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="de.nofail.dzone" 
      android:versionCode="1" android:versionName="0.0.1">
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>
    
    <uses-sdk android:minSdkVersion="8" />

    <application android:icon="@drawable/icon" android:label="@string/app_name">
        <activity android:name="ResultListActivity" android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="DetailsActivity" android:label="@string/app_name" />
        <activity android:name="PreferencesActivity" android:label="@string/app_name" />
        
        <uses-library android:name="android.test.runner" />
        
    </application>
    
    <instrumentation android:label="Unit Tests" android:targetPackage="de.nofail.dzone" android:name="de.nofail.dzone.TestRunner" />

	<uses-permission android:name="android.permission.INTERNET" />

</manifest> 